const db = require("../models");

module.exports ={
    login: function(req,res){
        // console.log("test");
        // passport.authenticate("local"), function(req, res) {
        //     res.json(req.user);
        //   };
    }
 }